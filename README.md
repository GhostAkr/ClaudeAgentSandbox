# claude_agent_sandbox
A sandbox for experiments with Claude agents.

A uv workspace with the following members:
- [`claude_api`](packages/claude_api) — an agent implemented with the Claude API.
- [`claude_sdk`](packages/claude_sdk) — an agent implemented with the Claude Agent SDK (placeholder).
